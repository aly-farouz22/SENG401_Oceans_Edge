model GameSave {
  id        Int      @id @default(autoincrement())
  userId    String   // associate with player account
  name      String   // save slot name
  data      Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}