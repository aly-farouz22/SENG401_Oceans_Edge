generator client {
  provider = "prisma-client-js"
}

model Player {
  id       String   @id @default(cuid())
  name     String
  money    Int      @default(0)
  fish     Json     @default("[]") // Store fish as JSON for now
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}